# ═══════════════════════════════════════════════════════
#  .env.example  —  Copy to .env for local testing
#  On Render: paste each into Environment Variables tab
# ═══════════════════════════════════════════════════════

# ── 1. BOT_TOKEN ────────────────────────────────────────
# From @BotFather on Telegram
# /newbot → name → username → copy token
BOT_TOKEN=7123456789:AAFxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ── 2. WEBHOOK_URL ──────────────────────────────────────
# Your Render app public URL (no trailing slash)
# Find it in Render dashboard after first deploy
WEBHOOK_URL=https://pw-auto-bot.onrender.com

# ── 3. API_ID ────────────────────────────────────────────
# From https://my.telegram.org → Log in → API Development Tools → Create App
API_ID=12345678

# ── 4. API_HASH ──────────────────────────────────────────
# Same page as API_ID
API_HASH=abcdef1234567890abcdef1234567890

# ── 5. SESSION_STRING ────────────────────────────────────
# Run: python gen_session.py (on YOUR LOCAL machine — NOT on server)
# Logs into your Telegram account as userbot
# ⚠️  NEVER share this — full account access
SESSION_STRING=BQAxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ── 6. SUDO_USERS ────────────────────────────────────────
# Your Telegram User ID (comma-separated if multiple)
# Get your ID: message @userinfobot on Telegram
SUDO_USERS=123456789

# ── 7. DB_PATH ───────────────────────────────────────────
# SQLite database location
# On Render: use /data/bot.db (persistent disk)
# Locally: bot.db (current folder)
DB_PATH=bot.db

# ── 8. PORT ──────────────────────────────────────────────
# Render sets this automatically — don't change on Render
PORT=10000
